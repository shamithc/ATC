 <h2>Create New Brand for <%= @item.name %></h2>

<%= form_for @brand, :url => create_brand_path, :html => { :class => 'form-horizontal' } do |f| %>
 <%= error_messages_for :brand %>
  <div><%= f.label :name %><br />
  <%= f.text_field :name %></div>
<br />


  <div><%= f.submit "Create" %></div>
<% end %>


